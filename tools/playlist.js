(()=>{'use strict';const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const key='vardhan-playlist-only-v1';let state={watched:[],theme:'light'};try{const s=JSON.parse(localStorage.getItem(key));if(s){state.watched=Array.isArray(s.watched)?s.watched:[];state.theme=s.theme==='dark'?'dark':'light';}}catch{}const save=()=>{try{localStorage.setItem(key,JSON.stringify(state));}catch{$('#storage-note').hidden=false;}};function paint(){document.body.classList.toggle('dark',state.theme==='dark');$('#theme').textContent=state.theme==='dark'?'Light mode':'Dark mode';const valid=$$('.complete').filter(b=>state.watched.includes(b.dataset.complete));$('#progress-count').textContent=valid.length+' / 56 watched';$('#progress').value=valid.length;$$('.complete').forEach(b=>{let yes=state.watched.includes(b.dataset.complete);b.textContent=yes?'✓ Watched · Undo':'Mark watched';b.setAttribute('aria-pressed',String(yes));});$$('nav a').forEach(a=>a.querySelector('.nav-check').textContent=state.watched.includes(a.dataset.page)?'✓':'');}let first=true;function open(){let page=document.getElementById(location.hash.slice(1));if(!page?.classList.contains('page'))page=$('#video-02');$$('.page').forEach(p=>p.hidden=p!==page);$$('nav a').forEach(a=>{if(a.dataset.page===page.id)a.setAttribute('aria-current','page');else a.removeAttribute('aria-current');});document.title=page.querySelector('h1').textContent+' — VLSI Learning Vardhan';document.body.classList.remove('nav-open');$('#menu').setAttribute('aria-expanded','false');window.scrollTo(0,0);if(!first)page.querySelector('h1').focus({preventScroll:true});first=false;}$('#search').addEventListener('input',e=>{let q=e.target.value.trim().toLowerCase(),n=0;$$('nav a').forEach(a=>{let show=!q||document.getElementById(a.dataset.page).textContent.toLowerCase().includes(q);a.hidden=!show;if(show)n++;});$('#search-status').textContent=q?n+' matching entries':'';});$$('.complete').forEach(b=>b.addEventListener('click',()=>{let id=b.dataset.complete;state.watched=state.watched.includes(id)?state.watched.filter(x=>x!==id):[...state.watched,id];save();paint();}));$('#theme').addEventListener('click',()=>{state.theme=state.theme==='dark'?'light':'dark';save();paint();});$('#print').addEventListener('click',()=>window.print());$('#menu').addEventListener('click',()=>{$('#menu').setAttribute('aria-expanded',String(document.body.classList.toggle('nav-open')));});document.addEventListener('keydown',e=>{if(e.key==='Escape'){document.body.classList.remove('nav-open');$('#menu').setAttribute('aria-expanded','false');}});window.addEventListener('hashchange',open);paint();open();})();
